# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - develop
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Make envfile
        run: |
          touch .env.production
          echo VITE_API_KEY="AIzaSyD20jPe8y4FuJUQSP9pPRdgZDcAjn0CCiA" >> .env
          echo VITE_AUTH_DOMAIN="preprod-bird-watcher.firebaseapp.com" >> .env
          echo VITE_PROJECT_ID="preprod-bird-watcher" >> .env
          echo VITE_STORAGE_BUCKET="preprod-bird-watcher.appspot.com" >> .env
          echo VITE_MESSAGING_SENDER_ID="940959059701" >> .env
          echo VITE_APP_ID="1:940959059701:web:6f3024e38fbdabd183ffbc" >> .env
          
          echo "cat .env"
          cat .env.production
      - uses: actions/checkout@v3
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PREPROD_BIRD_WATCHER }}'
          channelId: live
          projectId: preprod-bird-watcher
